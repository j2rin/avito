type: dict
keyschema:
  type: string
  regex: '^[a-z]+([a-z\d]+_|_[a-z\d]+)+[a-z\d]+$'
  minlength: 3
  maxlength: 64
valueschema:
  schema:
    template:
      type: string
    events:
      type: list
