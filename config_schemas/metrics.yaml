type: dict
keyschema:
  type: string
  regex: '^[a-z]+([a-z_\d]+)+[a-z\d]+$'
  minlength: 3
  maxlength: 64
valueschema:
  schema:
    template:
      type: string
    observations:
      type: list
    description:
      type: string
    params:
      type: string
