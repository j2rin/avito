metric.counter:
  calls:                                   {obs: [call_phone_screen_views_client, observation_value]}

metric.uniq:
  users_phone_calls_client:                {counter: calls,                           key: [participant]}

metric.ratio:
  calls_per_dialogue:                      {num: calls,                           den: calls_dialogues}

