metric.counter:
  map_local_geo:                           {obs: [district_map_searches, metro_map_searches, road_map_searches]}

metric.uniq:
  user_map_local_geo:                      {counter: map_local_geo,                   key: [participant]}

metric.ratio:
  map_local_geo_per_map:                   {num: map_local_geo,                   den: map_searches}

