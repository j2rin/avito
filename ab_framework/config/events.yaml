search_with_query:
  event_template: click_stream_events
  sql_bindings:
    event_types: 300
    filter_predicate: AND cs.searchquery_id IS NOT NULL

search_without_query:
  event_template: click_stream_events
  sql_bindings:
    event_types: 300
    filter_predicate: AND cs.searchquery_id IS NULL

item_view:
  event_template: click_stream_events
  sql_bindings:
    event_types: 301

phone_view:
  event_template: click_stream_events
  sql_bindings:
    event_types: 303
