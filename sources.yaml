buyer_stream:
  metric_configs: [buyer_stream]
  database: vertica
  table: dma.vo_buyer_stream
  column_map:
    query: query_id
    item: item_id
    session: [cookie_id, session_no]
    cookie: cookie_id
    user: user_id
    cookie_item: [cookie_id, item_id]
    item_x: [item_id, x]
    x: [cookie_id, x]
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - rec_engine
    - x_vertical
    - x_logical_category
    - x_category
    - x_subcategory
    - x_param1
    - x_param2
    - x_param3
    - x_param4
    - x_region
    - x_city
    - x_location_group
    - x_population_group
    - x_location_level
  participant: {visitor: cookie_id, user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  dtm: event_date
  segm: cookie_id

pageviews:
  metric_configs: [pageviews]
  database: vertica
  table: dma.vo_clickstream_pageviews
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

seller_item_active:
  metric_configs: [seller_item_active]
  database: vertica
  table: dma.vo_seller_item_active
  column_map:
    item: item_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - is_asd
    - user_segment_market
    - logical_param1
    - logical_param2
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
    - [is_logged_in, vertical, logical_category, logical_param1, logical_param2, region]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

seller_item_event:
  metric_configs: [seller_item_event]
  database: vertica
  table: dma.vo_seller_item_event
  column_map:
    item: item_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - is_asd
    - user_segment_market
    - location
    - microcat
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

geo_loc_change:
  metric_configs: [geo_loc_change]
  database: vertica
  table: dma.vo_geo_loc_change
  column_map:
    cookie: cookie_id
    session: session_no
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

geo_map_stream:
  metric_configs: [geo_map_stream]
  database: vertica
  table: dma.vo_geo_map_stream
  column_map:
    session: session_no
    cookie: cookie_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_mobile:
  metric_configs: [perf_mobile]
  database: vertica
  table: dma.o_perf_mobile
  dimensions: [platform, network_type, is_core_content, screen_name, content_type]
  participant: {visitor: cookie_id, user: user_id}
  dtm: observation_date
  segm: cookie_id

perf_mobile_apps_startup_times:
  metric_configs: [perf_mobile_apps_startup_times]
  database: vertica
  table: dma.o_perf_mobile_apps_startup_times
  dimensions: [platform]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_mobile_memory_warnings:
  metric_configs: [perf_mobile_memory_warnings]
  database: vertica
  table: dma.o_perf_memory_warnings
  column_map:
    cookie: cookie_id
  dimensions: [platform, screen_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_web:
  metric_configs: [perf_web]
  database: vertica
  table: dma.o_perf_web
  column_map:
    cookie: cookie_id
  dimensions: [platform, network_type, screen_name, stage_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: observation_date
  segm: cookie_id

core_contacts:
  metric_configs: [core_contacts]
  column_map:
    cookie: cookie_id
    logcat_item: [item_id, logical_category_id]
    cookie_logcat: [cookie_id, logical_category_id]
  database: vertica
  table: dma.vo_core_contacts
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, region, city, location_group, population_group, location_level, price_group, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

advert_funnel:
  metric_configs: [advert_funnel]
  column_map:
    session: session_hash
  database: vertica
  table: dma.vo_advert_funnel
  dimensions: [platform, category, subcategory, vertical, logical_category]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

mnz_bundle:
  metric_configs: [mnz_bundle]
  database: vertica
  table: dma.vo_mnz_bundle
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [vertical, logical_category]
    - [category, subcategory]
    - [is_asd, vertical, logical_category, user_segment_market]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

core_c2c:
  metric_configs: [core_c2c]
  database: vertica
  table: dma.vo_core_c2c
  column_map:
    user: user_id
    participant: participant_id
  dimensions: [platform, vertical, logical_category, category, subcategory, region, city, location_group, population_group, location_level, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

user_rating:
  metric_configs: [user_rating]
  database: vertica
  table: dma.vo_user_rating
  dimensions: [vertical, logical_category]
  m42_cartesian_groups:
    - [vertical, logical_category]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

str:
  metric_configs: [str]
  database: vertica
  table: dma.vo_str
  column_map:
    participant: participant_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

ratings:
  metric_configs: [ratings]
  database: vertica
  table: dma.vo_ratings
  column_map:
    participant: participant_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

public_profile:
  metric_configs: [public_profile]
  database: vertica
  table: dma.vo_public_profile
  column_map:
    cookie: cookie_id
  dimensions: [platform]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

mnz_vas:
  metric_configs: [mnz_vas]
  database: vertica
  table: dma.vo_mnz_vas
  column_map:
    user: user_id
    participant: participant_id
  dimensions: [platform, page_from, vertical, category, subcategory, logical_category, param1, param2, param3, param4, region, city, location_group, population_group, location_level, is_asd, user_segment_market]
  participant: {user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  dtm: event_date
  segm: cookie_id

mnz_lf:
  metric_configs: [mnz_lf]
  database: vertica
  table: dma.vo_mnz_lf
  dimensions: [platform, vertical, logical_category, category, subcategory, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

messenger:
  metric_configs: [messenger]
  database: vertica
  table: dma.vo_messenger
  column_map:
    cookie: cookie_id
    user: user_id
    participant: participant_id
  dimensions: [platform, appversion, vertical, category, subcategory, logical_category, param1, param2, param3, param4, region, city, location_group, population_group, location_level, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

labor:
  metric_configs: [labor]
  database: vertica
  table: dma.vo_labor
  column_map:
    cookie: cookie_id
    session: session_no
    participant: participant_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

express_cv:
  metric_configs: [express_cv]
  database: vertica
  table: dma.vo_express_cv
  dimensions: [platform]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

delivery:
  metric_configs: [delivery]
  database: vertica
  table: dma.vo_delivery
  column_map:
    participant: participant_id
  dimensions: [platform, vertical, category, subcategory, logical_category, is_intercity, param1, param2, param3, param4, region, city, location_group, population_group, location_level, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

core_b2b:
  metric_configs: [core_b2b]
  database: vertica
  table: dma.vo_core_b2b
  column_map:
    user: user_id
  dimensions: [platform, vertical, category, subcategory, logical_category, region, city, population_group, location_group, location_level, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

billing:
  metric_configs: [billing]
  database: vertica
  table: dma.vo_billing
  column_map:
    user: user_id
  dimensions: [platform]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

session_source:
  metric_configs: [session_source]
  database: vertica
  table: dma.vo_session_source
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant:  {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

support:
  metric_configs: [support]
  database: vertica
  table: dma.vo_support
  column_map:
    participant: participant_id
  dimensions: [platform]
  participant:  {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_web_speed:
  metric_configs: [perf_web_speed]
  database: vertica
  table: dma.vo_perf_web_speed
  column_map:
    participant: participant_id
  dimensions: [platform, region, city, location_group, from_page, population_group, location_level]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

cookie_account_enter:
  metric_configs: [cookie_account_enter]
  database: vertica
  table: dma.vo_cookie_account_enter
  column_map:
    cookie: cookie_id
  dimensions: [platform]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

favourite_sellers_totals:
  metric_configs: [favourite_sellers_totals]
  database: vertica
  table: dma.favourite_sellers_totals
  column_map:
    user: user_id
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

communications:
  metric_configs: [communications]
  database: vertica
  table: dma.vo_communications
  column_map:
    user: user_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - phone_location_group
    - phone_population_group
    - phone_location_level
    - phone_city
    - phone_region
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant:  {user: user_id}
  dtm: event_date
  segm: user_id

image:
  metric_configs: [image]
  database: vertica
  table: dma.vo_image
  column_map:
    participant: participant_id
  dimensions: [platform, network_type, screen_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

proprofile:
  metric_configs: [proprofile]
  database: vertica
  table: dma.vo_proprofile
  column_map:
    participant: participant_id
  dimensions: [platform, proprofile_traffic, proprofile_type, proprofile_version, tariff_type, tariff_subtype, tariffvertical_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

buyer_suggest:
  metric_configs: [buyer_suggest]
  database: vertica
  table: dma.vo_buyer_suggest
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  m42_cartesian_groups:
    - [is_logged_in]
  dtm: event_date
  segm: cookie_id

seller_item_add:
  metric_configs: [seller_item_add]
  database: vertica
  table: dma.vo_seller_item_add
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: eventchain_id

seller_item_add_finish:
  metric_configs: [seller_item_add_finish]
  database: vertica
  table: dma.vo_seller_item_add
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: item_create_date
  segm: eventchain_id

support_ticket:
  metric_configs: [support_ticket]
  database: vertica
  table: dma.vo_support_ticket
  participant: {user: user_id}
  dtm: create_date
  segm: user_id

geo_map_labor:
  metric_configs: [geo_map_labor]
  database: vertica
  table: dma.vo_geo_map_labor
  column_map:
    participant: participant_id
  dimensions:
    - platform
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, region]
  participant:  {visitor: cookie_id, user: user_id}
  dtm: event_Date
  segm: cookie_id

buyer_call:
  metric_configs: [buyer_call]
  database: vertica
  table: dma.vo_buyer_call
  column_map:
    item: item_id
    participant: participant_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

buyer_courier_order_create_date:
  metric_configs: [buyer_courier_order_create_date]
  database: vertica
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: buyer_id}
  dtm: create_date
  segm: courierorder_id

buyer_courier_order_status_date:
  metric_configs: [buyer_courier_order_status_date]
  database: vertica
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: buyer_id}
  dtm: status_date
  segm: courierorder_id

seller_courier_order_create_date:
  metric_configs: [seller_courier_order_create_date]
  database: vertica
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: seller_id}
  dtm: create_date
  segm: courierorder_id

seller_courier_order_status_date:
  metric_configs: [seller_courier_order_status_date]
  database: vertica
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: seller_id}
  dtm: status_date
  segm: courierorder_id

notification:
  metric_configs: [notification]
  database: vertica
  table: dma.vo_notification
  column_map:
    cookie: cookie_id
    participant: participant_id
  dimensions: [platform]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

calltracking:
  metric_configs: [calltracking]
  database: vertica
  table: dma.vo_calltracking
  column_map:
    user: user_id
  dimensions:
    - vertical
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, region]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

buyer_weighted:
  metric_configs: [buyer_weighted]
  database: vertica
  table: dma.vo_buyer_weighted
  dimensions: [platform, vertical, logical_category, logical_region, price_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id_event_date

app_call_bx:
  metric_configs: [app_call_bx]
  database: vertica
  table: dma.vo_app_call_buyer
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

app_call_sx:
  metric_configs: [app_call_sx]
  database: vertica
  table: dma.vo_app_call_seller
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

app_call_event:
  metric_configs: [app_call_event]
  database: vertica
  table: dma.vo_app_call_event
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

app_call_seller_event:
  metric_configs: [app_call_seller_event]
  database: vertica
  table: dma.vo_app_call_seller_event
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

delivery_shipments_by_create_date:
  metric_configs: [delivery_shipments_by_create_date]
  database: vertica
  table: dma.vo_delivery_shipments
  dimensions:
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [region]
  participant: {user: buyer_id}
  dtm: create_date
  segm: shipment_id

delivery_shipments_by_status_date:
  metric_configs: [delivery_shipments_by_status_date]
  database: vertica
  table: dma.vo_delivery_shipments
  dimensions:
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [region]
  participant: {user: buyer_id}
  dtm: status_date
  segm: shipment_id

buyer_delivery_shipment_items_by_create_date:
  metric_configs: [buyer_delivery_shipment_items_by_create_date]
  database: vertica
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
    user: buyer_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: buyer_id}
  dtm: create_date
  segm: shipment_id

buyer_delivery_shipment_items_by_status_date:
  metric_configs: [buyer_delivery_shipment_items_by_status_date]
  database: vertica
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
    user: buyer_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: buyer_id}
  dtm: status_date
  segm: shipment_id

seller_delivery_shipment_items_by_create_date:
  metric_configs: [seller_delivery_shipment_items_by_create_date]
  database: vertica
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
    user: seller_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: seller_id}
  dtm: create_date
  segm: shipment_id

seller_delivery_shipment_items_by_status_date:
  metric_configs: [seller_delivery_shipment_items_by_status_date]
  database: vertica
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
    user: seller_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - platform
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {user: seller_id}
  dtm: status_date
  segm: shipment_id

fraud_support_tickets:
  metric_configs: [fraud_support_tickets]
  database: vertica
  table: dma.vo_fraud_support_tickets
  column_map:
    cookie: cookie_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
  m42_cartesian_groups:
    - [vertical, logical_category]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: create_date

autoteka_stream:
  metric_configs: [autoteka_stream]
  database: vertica
  table: dma.autoteka_stream
  dimensions: [platform, payment_method, reports_count, searchtype, is_authorized, is_new_user]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

geo_coordinates_stream:
  metric_configs: [geo_coordinates_stream]
  database: vertica
  table: dma.vo_coordinates_stream
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
      - [is_logged_in]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

geo_laas_backend:
  metric_configs: [geo_laas_backend]
  database: vertica
  table: dma.vo_laas_stream
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
      - [is_logged_in]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

moder:
  metric_configs: [moder]
  database: vertica
  table: dma.vo_moder
  dimensions: [microcat]
  participant: {user: participant_id}
  dtm: event_date

callcenter_calls:
  metric_configs: [callcenter_calls]
  database: vertica
  table: dma.vo_callcenter_calls
  participant: {user: user_id}
  dtm: enter_date

lf_metrics_transactions_by_event_date:
  metric_configs: [lf_metrics_transactions_by_event_date]
  database: vertica
  table: dma.vo_lf_metrics_transactions
  column_map:
    user: user_id
  dimensions:
    - user_segment_market
    - tariff_type
    - tariff_subtype
    - platform
    - vertical
    - logical_category
    - region
    - city
    - location_group
    - population_group
    - location_level
  participant: {user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
  dtm: event_date
  segm: user_id

lf_metrics_by_event_date:
  metric_configs: [lf_metrics_by_event_date]
  database: vertica
  table: dma.vo_lf_metrics
  column_map:
    user: user_id
  dimensions:
    - user_segment_market
    - is_asd
    - tariff_type
    - tariff_subtype
    - vertical
    - logical_category
    - region
    - city
    - location_group
    - population_group
    - location_level
  participant: {user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
  dtm: event_date
  segm: user_id

tariff_metrics_funnel_by_event_date:
  metric_configs: [tariff_metrics_funnel_by_event_date]
  database: vertica
  table: dma.vo_tariff_metrics_funnel
  column_map:
    user: user_id
  dimensions:
    - user_segment_market
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
  participant: {user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
    - [category, subcategory]
  dtm: event_date
  segm: user_id

paying_user_report_by_event_date:
  metric_configs: [paying_user_report_by_event_date]
  database: vertica
  table: dma.vo_revenue_metrics
  column_map:
    user: user_id
  dimensions:
    - user_segment_market
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  participant: {user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  dtm: event_date
  segm: user_id

support_comment:
  metric_configs: [support_comment]
  database: vertica
  table: dma.vo_support_comment
  participant: {user: comment_id}
  dtm: comment_create_date

time_to_interview_for_job_seeker:
  metric_configs: [time_to_interview_for_job_seeker]
  database: vertica
  table: dma.vo_time_to_interview
  column_map:
    user: job_seeker_user_id
  dimensions:
    - platform
  participant: {user: job_seeker_user_id}
  dtm: event_time
  segm: job_seeker_user_id

time_to_interview_for_job_employer:
  metric_configs: [time_to_interview_for_job_employer]
  database: vertica
  table: dma.vo_time_to_interview
  column_map:
    user: job_employer_user_id
  dimensions:
    - platform
  participant: {user: job_employer_user_id}
  dtm: event_time
  segm: job_employer_user_id

session_key_event_type:
  metric_configs: [session_key_event_type]
  database: vertica
  table: dma.vo_session_key_event_type
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  participant: {visitor: cookie_id, user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  dtm: event_date
  segm: cookie_id

search_items:
  metric_configs: [search_items]
  database: vertica
  table: dma.vo_search_items
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - region
  participant: {visitor: cookie_id}
  m42_cartesian_groups:
    - [vertical, logical_category, region]
    - [category, subcategory]
  dtm: event_date
  segm: event_date  # Просто рандомный столбец, не совпадающий с cookie_id. Чтобы вынудить сделать материализацию при расчете витрин

production_errors:
  metric_configs: [production_errors]
  database: vertica
  table: dma.vo_production_errors
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

geo_loc_suggest:
  metric_configs: [geo_loc_suggest]
  database: vertica
  table: dma.vo_geo_loc_suggest
  column_map:
    cookie: cookie_id
  dimensions:
    - platform
    - location_group
    - population_group
    - location_level
  participant: {visitor: cookie_id}
  dtm: event_date

clickstream:
  metric_configs: [clickstream]
  database: vertica
  table: dma.vo_clickstream_new
  column_map:
    cookie: cookie_id
  dimensions: [platform, vertical, logical_category, category, region, city, location_group, population_group,
               location_level, subcategory, param1, param2, param3, param4]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

clickstream_counters:
  metric_configs: [clickstream_counters]
  database: vertica
  table: dma.vo_clickstream_counters
  column_map:
    cookie: cookie_id
  dimensions: [platform, vertical, logical_category, category, region, city, location_group, population_group,
               location_level, subcategory, param1, param2, param3, param4]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

messenger_stream:
  metric_configs: [messenger_stream]
  database: vertica
  table: dma.vo_messenger_stream
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, category, subcategory, logical_category, param1, param2, param3, param4, region, city, location_group, population_group, location_level, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

clickstream_sparse.clickhouse:
    database: clickhouse
    table: dwh.clickstream_sparse_local
    column_map:
      platform_id: business_platform
      cookie: u
      item: iid
      item_x: [iid, x]
      x_eid: __x_eid
    dtm: event_date
    participant: {visitor: u, user: uid}
    metrics:
      - bt_clicks
      - bt_clicks_grouped
      - buyers
      - contacts
      - contacts_grouped
      - user_pv
      - users_bt_click_daily
      - users_first_messages
