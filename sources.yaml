buyer_stream:
  table: dma.vo_buyer_stream
  column_map:
    query: query_id
    item: item_id
    session: session_no
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - rec_engine
    - x_vertical
    - x_logical_category
    - x_category
    - x_subcategory
    - x_param1
    - x_param2
    - x_param3
    - x_param4
    - x_region
    - x_city
    - x_location_group
    - x_population_group
    - x_location_level
  participant: {visitor: cookie_id, user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  dtm: event_date
  segm: cookie_id

click_stream:
  table: dma.vo_clickstream
  column_map:
    session: session_no
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

pageviews:
  table: dma.vo_clickstream_pageviews
  column_map:
    cookie: cookie_id
    user: user_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

seller_item_active:
  table: dma.vo_seller_item_active
  column_map:
    item: item_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - user_segment
    - is_asd
    - user_segment_market
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

seller_item_event:
  table: dma.vo_seller_item_event
  column_map:
    item: item_id
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - user_segment
    - is_asd
    - user_segment_market
    - location
    - microcat
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

geo_loc_change:
  table: dma.vo_geo_loc_change
  column_map:
    session: session_no
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

geo_map_stream:
  table: dma.vo_geo_map_stream
  column_map:
    session: session_no
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_mobile:
  table: dma.o_perf_mobile
  dimensions: [platform, network_type, is_core_content, screen_name, content_type]
  participant: {visitor: cookie_id, user: user_id}
  dtm: observation_date
  segm: cookie_id

perf_mobile_memory_warnings:
  table: dma.o_perf_memory_warnings
  dimensions: [platform, screen_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_web:
  table: dma.o_perf_web
  dimensions: [platform, network_type, screen_name, stage_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: observation_date
  segm: cookie_id

core_contacts:
  table: dma.vo_core_contacts
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, region, city, location_group, population_group, location_level, user_segment, price_group, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id_event_date

advert_funnel:
  column_map:
    session: session_hash
  table: dma.vo_advert_funnel
  dimensions: [platform, category, subcategory, vertical, logical_category]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

mnz_bundle:
  table: dma.vo_mnz_bundle
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, user_segment, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [vertical, logical_category]
    - [category, subcategory, param1]
    - [user_segment, vertical, logical_category]
    - [is_asd, vertical, logical_category, user_segment_market]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

core_c2c:
  table: dma.vo_core_c2c
  dimensions: [platform, vertical, logical_category, category, subcategory, region, city, location_group, population_group, location_level, user_segment, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

user_rating:
  table: dma.vo_user_rating
  dimensions: [vertical, logical_category]
  m42_cartesian_groups:
    - [vertical, logical_category]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

str:
  table: dma.vo_str
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

ratings:
  table: dma.vo_ratings
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

public_profile:
  table: dma.vo_public_profile
  dimensions: [platform]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

mnz_vas:
  table: dma.vo_mnz_vas
  dimensions: [platform, page_from, vertical, category, subcategory, logical_category, param1, param2, param3, param4, region, city, location_group, population_group, location_level, user_segment, is_asd, user_segment_market]
  participant: {visitor: cookie_id, user: user_id}
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  dtm: event_date
  segm: cookie_id

mnz_lf:
  table: dma.vo_mnz_lf
  dimensions: [platform, vertical, logical_category, category, subcategory, region, city, location_group, population_group, location_level]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory]
    - [vertical, logical_category, region, city]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

messenger:
  table: dma.vo_messenger
  dimensions: [platform, appversion, vertical, category, subcategory, logical_category, param1, param2, param3, param4, region, city, location_group, population_group, location_level, user_segment, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

labor:
  table: dma.vo_labor
  column_map:
    session: session_no
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category]
    - [category, subcategory, param1]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

express_cv:
  table: dma.vo_express_cv
  dimensions: [platform]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

delivery:
  table: dma.vo_delivery
  dimensions: [platform, vertical, category, subcategory, logical_category, is_intercity, param1, param2, param3, param4, region, city, location_group, population_group, location_level, user_segment, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

core_b2b:
  table: dma.vo_core_b2b
  dimensions: [platform, vertical, category, subcategory, logical_category, region, city, population_group, location_group, location_level, user_segment, is_asd, user_segment_market]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category, region]
    - [is_logged_in, is_asd, vertical, logical_category, region, user_segment_market]
    - [category, subcategory]
    - [vertical, logical_category, region, city]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

billing:
  table: dma.vo_billing
  dimensions: [platform]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

session_source:
  table: dma.vo_session_source
  dimensions:
    - platform
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant:  {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

support:
  table: dma.vo_support
  dimensions: [platform]
  participant:  {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

perf_web_speed:
  table: dma.vo_perf_web_speed
  dimensions: [platform, region, city, location_group, from_page, population_group, location_level]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

cookie_account_enter:
  table: dma.vo_cookie_account_enter
  dimensions: [platform]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id

favourite_sellers_totals:
  table: dma.favourite_sellers_totals
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

communications:
  table: dma.vo_communications
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
    - phone_location_group
    - phone_population_group
    - phone_location_level
    - phone_city
    - phone_region
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant:  {user: user_id}
  dtm: event_date
  segm: user_id

image:
  table: dma.vo_image
  dimensions: [platform, network_type, screen_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

proprofile:
  table: dma.vo_proprofile
  dimensions: [platform, proprofile_traffic, proprofile_type, proprofile_version, tariff_type, tariff_subtype, tariffvertical_name]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

buyer_suggest:
  table: dma.vo_buyer_suggest
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

seller_item_add:
  table: dma.vo_seller_item_add
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: eventchain_id

seller_item_add_finish:
  table: dma.vo_seller_item_add
  dimensions:
    - platform
  participant: {visitor: cookie_id, user: user_id}
  dtm: item_create_date
  segm: eventchain_id

support_ticket:
  table: dma.vo_support_ticket
  participant: {user: user_id}
  dtm: create_date
  segm: user_id

geo_map_labor:
  table: dma.vo_geo_map_labor
  dimensions:
    - platform
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, region]
    - [region, city]
  participant:  {visitor: cookie_id, user: user_id}
  dtm: event_Date
  segm: cookie_id

buyer_call:
  table: dma.vo_buyer_call
  column_map:
    item: item_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

buyer_courier_order_create_date:
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: buyer_id}
  dtm: create_date
  segm: courierorder_id

buyer_courier_order_status_date:
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: buyer_id}
  dtm: status_date
  segm: courierorder_id

seller_courier_order_create_date:
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: seller_id}
  dtm: create_date
  segm: courierorder_id

seller_courier_order_status_date:
  table: dma.vo_courier_orders
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: seller_id}
  dtm: status_date
  segm: courierorder_id

notification:
  table: dma.vo_notification
  dimensions: [platform]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: cookie_id

calltracking:
  table: dma.vo_calltracking
  dimensions:
    - vertical
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, region]
    - [vertical, region, city]
  participant: {user: user_id}
  dtm: event_date
  segm: user_id

buyer_weighted:
  table: dma.vo_buyer_weighted
  dimensions: [platform, vertical, logical_category, logical_region, user_segment, price_group]
  m42_cartesian_groups:
    - [is_logged_in, user_segment, vertical, logical_category]
  participant: {visitor: cookie_id}
  dtm: event_date
  segm: cookie_id_event_date

app_call_bx:
  table: dma.vo_app_call_buyer
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

app_call_sx:
  table: dma.vo_app_call_seller
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

app_call_event:
  table: dma.vo_app_call_event
  column_map:
    item: item_id
    cookie: cookie_id
    user: user_id
  dimensions: [platform, vertical, logical_category, category, subcategory, param1, param2, param3, param4, region, city, population_group]
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date
  segm: event_date

delivery_shipments_by_create_date:
  table: dma.vo_delivery_shipments
  dimensions:
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [region, city]
  participant: {user: buyer_id}
  dtm: create_date
  segm: shipment_id

delivery_shipments_by_status_date:
  table: dma.vo_delivery_shipments
  dimensions:
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [region, city]
  participant: {user: buyer_id}
  dtm: status_date
  segm: shipment_id

buyer_delivery_shipment_items_by_create_date:
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: buyer_id}
  dtm: create_date
  segm: shipment_id

buyer_delivery_shipment_items_by_status_date:
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: buyer_id}
  dtm: status_date
  segm: shipment_id

seller_delivery_shipment_items_by_create_date:
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: seller_id}
  dtm: create_date
  segm: shipment_id

seller_delivery_shipment_items_by_status_date:
  table: dma.vo_delivery_shipment_items
  column_map:
    item: item_id
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
    - param1
    - param2
    - param3
    - param4
    - region
    - city
    - location_group
    - population_group
    - location_level
  m42_cartesian_groups:
    - [is_logged_in, vertical, logical_category, region]
    - [category, subcategory, param1]
    - [vertical, logical_category, region, city]
  participant: {user: seller_id}
  dtm: status_date
  segm: shipment_id

fraud_support_tickets:
  table: dma.vo_fraud_support_tickets
  dimensions:
    - vertical
    - logical_category
    - category
    - subcategory
  m42_cartesian_groups:
    - [vertical, logical_category]
    - [category, subcategory]
  participant: {visitor: cookie_id, user: user_id}
  dtm: create_date

autoteka_stream:
  table: dma.autoteka_stream
  dimensions: [payment_method, reports_count, searchtype, is_authorized, is_new_user]
  participant: {visitor: cookie_id, user: user_id}
  dtm: event_date

geo_coordinates_stream:
  table: dma.vo_coordinates_stream
  dimensions:
    - platform
    - location_group
    - population_group
    - location_level
  participant: {visitor: cookie_id, user: user_id}

moder:
  table: dma.vo_moder
  dimensions: [microcat]
  participant: {user: participant_id}
  dtm: event_date

callcenter_calls:
  table: dma.vo_callcenter_calls
  participant: {user: user_id}
  dtm: enter_date