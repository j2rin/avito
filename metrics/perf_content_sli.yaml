metric.counter:
  successful_requests:                     {obs: [success_requests]}
  failed_requests:                         {obs: [failed_requests]}
  all_requests:                            {obs: [failed_requests, success_requests]}

metric.ratio:
  content_requests_sli:                    {num: successful_requests, den: all_requests}
